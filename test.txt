# Borrar repositorio Backend
aws ecr delete-repository --repository-name ulises-calculadora-backend --force

# Borrar repositorio Frontend
aws ecr delete-repository --repository-name ulises-calculadora-frontend --force

# 1. Quitar la política de ECR del rol
aws iam detach-role-policy --role-name github-actions-calculadora-rol --policy-arn arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryPowerUser

# 2. Borrar el rol
aws iam delete-role --role-name github-actions-calculadora-rol

